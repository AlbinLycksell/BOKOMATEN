[project]
name = "switchboard-scripts"
version = "0.1.0"
description = "Exploration scripts. Isolated from the production backend venv."
requires-python = ">=3.12"
dependencies = [
    "google-genai>=2.0",
    "opencv-python>=4.10",
    "sounddevice>=0.5",
    "pillow>=11",
    "mss>=10",
    "numpy>=2",
    "websockets>=13",
    "python-ulid>=3",
]

[tool.uv]
package = false
