[project]
name = "svarsa-scripts"
version = "0.1.0"
description = "Exploration scripts. Isolated from the production backend venv."
requires-python = ">=3.12"
dependencies = [
    "google-genai>=2.0",
    "opencv-python>=4.10",
    "pyaudio>=0.2.14",
    "pillow>=11",
    "mss>=10",
    "numpy>=2",
]

[tool.uv]
package = false
